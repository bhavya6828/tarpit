{
  "name": "tarpit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Autonomous AI scam-baiter. Wastes scammer time, extracts threat intel.",
  "scripts": {
    "dev": "concurrently -n SERVER,WEB -c magenta,cyan \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "node --env-file=.env server/index.js",
    "dev:web": "npm --prefix web run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix web run build",
    "start": "concurrently -n SERVER,WEB -c magenta,cyan \"node --env-file=.env server/index.js\" \"npm --prefix web run start\"",
    "simulate": "node --env-file=.env scripts/simulate-call.mjs",
    "ambience": "node --env-file=.env scripts/generate-ambience.mjs",
    "voicelab": "node --env-file=.env scripts/voice-lab.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
